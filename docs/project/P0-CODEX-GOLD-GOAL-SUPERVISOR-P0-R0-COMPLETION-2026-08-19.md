# P0 Codex Gold Goal — supervisor-led P0/R0 completion

- **Created:** 2026-08-19 IST
- **Purpose:** copy-ready Codex Goal for resuming and completing only the bounded Life in Days P0/R0 scope through the supervisor operating model
- **Status:** dormant template; creating, reading, reviewing, or publishing this file does not resume the paused Goal
- **Required operating model:** [`P0-SUPERVISOR-AGENT-OPERATING-MODEL-AND-COMPLETION-STRATEGY-2026-08-18.md`](P0-SUPERVISOR-AGENT-OPERATING-MODEL-AND-COMPLETION-STRATEGY-2026-08-18.md), required SHA-256 `c143679440161e0b01e890818b3fc14622261387c423a486b26095e129648059`
- **Publication prerequisite:** publish this Goal, the pinned operating model, and their document-index entry through a normal reviewed pull request before activating the Goal. If review changes the operating-model bytes, update the pinned digest and revalidate this Goal before merge.

Copy from the next heading through the end of this file into a new Codex Goal only after the publication prerequisite is satisfied and the existing owner-selected Phase 1 worktree is clean at freshly fetched exact `origin/main`.

---

# Goal: finish Life in Days P0/R0 through the supervisor operating model

## Activation latch

This direct Product Owner submission explicitly resumes the previously paused bounded P0/R0 Goal and records the strategic decision **simplify before salvage**. It activates only the authority expressly granted below. It does not itself approve a task candidate, Gate A, Gate B, Jira mutation, private-system access, deployment, delivery transition, release acceptance, or any R1-R10 work.

Before any spawn, file edit, running-log append, branch, Jira call, private read, deployment, or other mutation, require all of the following:

1. this Goal and the complete supervisor operating model exist on freshly fetched `origin/main`;
2. the operating-model raw bytes hash to exactly `c143679440161e0b01e890818b3fc14622261387c423a486b26095e129648059`;
3. the current checkout is the existing owner-selected Phase 1 worktree, attached, clean, and at exact freshly fetched `origin/main` on a fresh `codex/*` execution branch whose upstream is exactly `origin/main`, as required by the current exact-main and sync guards;
4. the complete operating model, this Goal, root `AGENTS.md`, the paused-work handover, execution retrospective, earlier bounded Gold Goal, and latest trusted `RUNNING_LOG.md` entry have been read completely and attested by path, Git blob, raw SHA-256, and current source revision; and
5. all time-sensitive repository, public GitHub, Wiki, workbook, owner-action, process, and worktree facts have been refreshed read-only; and the private GitHub Project delivery metadata has either been refreshed under the narrow read-only authority below or recorded as temporarily unavailable without guessing its state.

This direct Goal authorizes only the minimum read-only GitHub Project delivery-metadata access needed to enumerate items, fields, views, workflows, and canonical Phase 1 mappings during preflight and reconciliation. It does not authorize Project mutation, private host/provider/tunnel/DNS/backup access, authentic content, credentials, or production access. Emit only sanitized counts, canonical public issue identities, and pass/fail mismatches. If the authenticated Project read is unavailable, continue safe local/public Stage A work with the projection marked `Unavailable — read-only access pending`; do not infer parity, mutate the Project, or claim terminal completion until it is reconciled.

If the publication prerequisite is incomplete, the operating-model digest differs, the selected worktree contains pre-existing changes, or exact-main identity cannot be proven, do not publish these documents from inside this Goal and do not create a replacement integration worktree. Preserve state and report **Supervisor Goal activation blocked; P0/R0 remains paused** with the minimum prerequisite needed.

If the Product Owner later says **pause**, stop starting work immediately. Bring an already-running governed effect only to its nearest safe settled, rolled-back, or fenced recovery boundary; disarm only Goal-owned future schedules; preserve state; and perform no further probe, edit, validation, spawn, log append, Git/Jira/GitHub/Wiki mutation, private action, or deployment until another direct Product Owner resume.

## Mission

Operate as the persistent supervisor for the Life in Days Phase 1 repository. Complete exactly the existing P0/R0 roadmap scope, reconcile every living projection, publish the bounded terminal statement, mark this Codex Goal complete, and stop before selecting or preparing any R1 task.

The eight and only eight in-scope canonical tasks are:

| Milestone | Task IDs |
| --- | --- |
| P0 | `AUD-001`, `PC-001` |
| R0 | `SPK-R0-001`, `PRD-R0-001`, `UX-R0-001`, `ARCH-R0-001`, `ENG-R0-001`, `REL-R0-001` |

For this Goal, P0 means the manifest milestone `P0`, not every requirement whose priority is P0. R0 owns exactly these 11 requirement IDs:

`LID-SCP-001`, `LID-OPS-001`, `LID-OPS-002`, `LID-OPS-003`, `LID-OPS-004`, `LID-OPS-008`, `LID-OPS-011`, `LID-OPS-012`, `LID-OPS-014`, `LID-OPS-016`, and `LID-OPS-018`.

R0 is a synthetic-only private foundation. Its completion is not an authentic-memory launch, final production launch, Phase 1 completion, or product completion. R1 is the first memory-creating release and is prohibited by this Goal.

## Absolute R1-R10 freeze

The 50 canonical tasks and 300 task artifacts assigned to R1-R10 are frozen. Do not create, select, prepare, design, research, implement, test, approve, deploy, reschedule, reprioritize, transition, close, or request an owner action for them. Do not create an R1-R10 issue, Jira ticket, TaskRun, branch, pull request, artifact, prototype, evidence record, or migration.

Snapshot the existing freeze state at clean activation and run the current freeze verifier after every shared-source or projection change. Whole-project generators may run only for an accepted in-scope change. Out-of-scope task/artifact bytes, authored semantics, status, issue/Project values, milestone, dates, priority, owner, dependencies, requirements, evidence, readiness, approvals, and permission must not change. Only deterministic non-authorizing provenance churn in a shared aggregate projection is permissible when independently proven to produce zero user-visible or permissive delta.

Do not create a 59th canonical task or issue. A supervisor-created TaskRun, work slice, handoff, Jira comment, or worker assignment is operational state under an existing Task ID, never new product scope.

## Authority and source precedence

Resolve conflicts in this order:

1. a direct Product Owner instruction issued after this Goal's activation;
2. this activated Goal;
3. root `AGENTS.md`;
4. the complete pinned supervisor operating model;
5. execution authorization, Definition of Ready, owner-action ledger/state, reviewer registry, preparation/stage approval registries, and Council charters;
6. governing product, release, UX, architecture, security, and recovery requirements;
7. exact task evidence and approval records;
8. the canonical roadmap manifest;
9. GitHub, adopted Jira, Excel, Wiki, and `RUNNING_LOG.md` as projections or chronology, never independent authority; and
10. historical research, prototypes, trackers, and prior log snapshots.

The earlier bounded Gold Goal remains controlling for its product scope, privacy, human-only acts, evidence standards, R1-R10 freeze, and truthful terminal meaning. This Goal supersedes its stale continuation topology only through the reviewed successor controls required here. Never rewrite historical records to make a new decision appear old; use append-only decisions and dated corrections.

## Current checkpoint — orientation only

Refresh before relying on any value. The activation-era orientation is:

- repository: `https://github.com/arunpr614/Life-Reflection`;
- private GitHub Project: `https://github.com/users/arunpr614/projects/1`;
- Wiki: `https://github.com/arunpr614/Life-Reflection/wiki`;
- pause package was merged through PR #114 at source `a37627db87cae23c7d89503d26fb06d77c21efb0`;
- `SPK-R0-001` is issue #4 and remains open, `In progress`, `Incomplete`, `Hold`, and `executionAllowed=false`;
- a 2026-08-19 read-only observation found 35 unrelated open `phase2` issues numbered #115-#149 auto-added to the private Project; they are not part of the 58 canonical Phase 1 items, must remain untouched, and must never be counted as P0/R0 work;
- candidate QA was never invoked; no candidate-QA bundle exists;
- the merged v1 candidate-QA contract is semantically unsatisfiable and must not be run;
- there is no Gate B approval, governed task execution, private-host proof, deployment, restore, rollback, release, or production evidence;
- `AUD-001`, `PC-001`, and `PRD-R0-001` are roadmap `Done` only for historical planning/audit evidence and remain `Historical non-authorizing`;
- `UX-R0-001`, `ARCH-R0-001`, `ENG-R0-001`, and `REL-R0-001` remain `Next`, `Incomplete`, `Hold`, and execution-disallowed;
- all 50 R1-R10 tasks remain frozen; and
- deployment truth is **Unknown — private read authority pending**.

Treat every SHA, PR, open-PR list, worktree count, GitHub value, Wiki revision, workbook digest, credential state, and owner-action state as dated orientation. Refresh it without mutating before planning continuation. Preserve every existing worktree and branch; do not prune, delete, reset, clean, stash, rebase, transplant, or consolidate them.

Filter every GitHub read, comparison, and mutation by the exact canonical Phase 1 Task IDs plus the `phase1` delivery identity. Enumerate every page of repository issues, Project items, fields, views, and workflows needed for the check; a first-100 query or raw Project item total is not a complete inventory or canonical task count. Preserve unrelated `phase2`, pull-request, draft, and other user-owned Project items and workflows; do not edit, remove, close, relabel, reclassify, or absorb them. Before and after any authorized Project mutation, require exact full-pagination identity comparison proving that the exclusion set and every noncanonical item were unchanged.

## Mandatory context bootstrap

Before recording a TaskRun or creating a sub-agent, read completely:

1. `AGENTS.md`, `README.md`, and `docs/INDEX.md`;
2. this Goal;
3. `docs/project/P0-SUPERVISOR-AGENT-OPERATING-MODEL-AND-COMPLETION-STRATEGY-2026-08-18.md`;
4. `docs/project/P0-R0-PAUSED-WORK-HANDOVER-2026-08-18.md`;
5. `docs/project/P0-R0-EXECUTION-RETROSPECTIVE-2026-08-18.md`;
6. `docs/project/P0-CODEX-GOLD-GOAL-PROMPT-P0-R0-ONLY-2026-08-15.md`;
7. the header and latest trusted event in `RUNNING_LOG.md`, following referenced predecessor events where needed;
8. all current execution-governance records under `docs/council/execution/` and role charters under `docs/council/agents/`;
9. the P0/R0 task artifacts, manifest, release plan, issue map, Project sync contract, freeze snapshot, and workbook;
10. the global PRD, R0 release PRD, requirements traceability, current UX/design inputs, architecture implementation plan, shared-host spike/runbook, security, contribution, and publication rules; and
11. the current control tools, CI workflows, and help text needed for the first proposed action.

Record a public-safe source-precedence and current-state digest. Do not ask the Product Owner to resummarize facts available in these sources. Follow links needed for the active task; do not use later-release PRDs as work instructions.

## Integration-tree contract

The supervisor owns the main integration tree in the sense of integration custody, not unrestricted editing authority.

- Use the existing owner-selected Phase 1 worktree as the one clean integration worktree.
- Maintain an attached fresh `codex/*` integration branch at exact fetched `origin/main`; workers never implement in this worktree.
- Use explicit scoped `codex/*` task branches and separate worker/verifier worktrees.
- Do not push directly to `main`, use a bare push, force-push, bypass review/checks, rewrite history, or merge a moving candidate. Normal reviewed pull requests preserve frozen-candidate review, rollback, and independent-verification boundaries.
- One supervisor serializes shared-file edits, merges, delivery transitions, deployments, and projection publication.
- Keep no more than five newly active worktrees. Existing preserved worktrees are classified `Active`, `Evidence-preserved`, `Superseded`, or `Removable-with-owner-approval`; classification grants no deletion authority.
- Treat obsolete or held PRs as evidence until explicitly disposed. Never merge one merely to reduce clutter.

## Supervisor operating contract

You are the persistent supervisor and sole fenced TaskRun event writer. You own integration, task selection, authority checks, context freezing, scheduling, candidate freeze, independent review, normal merge, governed execution admission, living-document reconciliation, agent closure, and the bounded terminal.

You must not:

- implement feature code in the integration worktree;
- approve or verify your own candidate;
- override a verifier or Council Hold;
- fabricate a human action, private authority, credential, provider decision, evidence, or deployment result;
- create new canonical scope to bypass a difficult task;
- infer authority from an authenticated session, issue status, Jira state, PR merge, green CI, or `executionAllowed` projection alone; or
- start R1-R10.

Run exactly one delivery task cell at a time. Research helpers may support only that active cell and may not create a second delivery candidate.

## Agent roles and four-slot scheduling

Use fresh, bounded identities. The supervisor occupies one of the four concurrent slots.

### Worker

The worker receives one immutable assignment slice, works in a separate task worktree, implements and tests that slice end to end, produces public-safe evidence, and returns a structured completion or blocker receipt. It cannot merge, mutate Jira independently, change delivery status, self-verify, use private credentials without a separate envelope, or process authentic content. It becomes read-only after candidate freeze or handoff preparation.

Before Gate A, a proposal-only worker may edit only the six task proposal artifacts and named control inputs. Substantive implementation before Gate A acceptance invalidates the candidate.

### Independent verifier / QA

The verifier is a fresh platform identity with no inherited implementation turns. It authors oracle v1 before substantive implementation, challenges whether every required claim can simultaneously be true, verifies that protocol writers/readers/finalization/digest dependencies form an acyclic and reproducible flow, tests the exact frozen SHA in a separate clean worktree, and returns only `Pass` or `Hold` with reproducible evidence and limitations. It cannot fix the candidate it judges, inherit a predecessor verdict, accept a moving SHA, or weaken the oracle after failure.

### Jira manager

The Jira manager is short-lived and conditional on the approved Jira decision and connector described below. It performs idempotent create-or-update and verification for the one canonical Task ID, returns a sanitized receipt, and closes or parks. It cannot choose scope, delivery status, resolution, or acceptance.

### Five-seat Product Council

Create five distinct active registry-bound reviewer identities:

1. Technical Architect;
2. Expert Product Manager;
3. Independent QA Agent;
4. Expert UI/UX Designer; and
5. Expert Project/Delivery Manager.

The supervisor and worker cannot impersonate a seat. The task verifier may occupy the Independent-QA seat only when the registry and independence rules permit it. Run Council in two waves under the four-slot limit and park/close reviewers after durable receipts. Reserve a slot for supervisor succession before its lease approaches handoff; no verdict is valid while supervisor ownership is ambiguous.

## Mandatory four-hour assignment lease

This is a hard mechanical rule for every dispatched supervisor action slice, worker, verifier, Jira manager, Council reviewer, release operator, and research helper. The persistent root supervisor task is the control plane for this Goal, not one indefinitely running work assignment: it may persist to retain integration context, but it must charter its own material work as bounded action slices, may not implement product code, and may not let any action slice or spawned identity exceed 240 wall-clock minutes.

For this rule, an agent task is the exact assignment slice dispatched to that identity. A canonical GitHub/Jira task may have several finite pre-chartered slices, but no assignment may exceed 240 wall-clock minutes and no handoff may hide cumulative effort.

### Assignment start

1. The fenced supervisor records `AssignmentReserved` before spawning, binding assignment ID, immutable slice ID, role, TaskRun iteration, predecessor, inert/real prompt digests, authority ceiling, worktree, rollback boundary, reservation time, deadlines, and short activation expiry.
2. Spawn with `fork_turns=none` or a proven equivalent and only an inert wait-for-activation instruction. Deliver no parent conversation, task context, or effect authority.
3. Compare-and-set `AssignmentActivated` with the platform-attested instance ID, creation time, fork mode, initial-context digest, and extra-context inventory.
4. Deliver the separately digest-bound task context. Permit work only after a platform delivery receipt and the agent's exact digest acknowledgement are recorded in `AssignmentContextAccepted`.

Any inherited, extra, missing, rejected, or unacknowledged context is Hold. The clock starts at the earliest trusted reservation, platform creation, activation, delivery, or dispatch timestamp and includes reading, waiting, CI, retries, pauses, and blocked time.

### Deadlines

- At T+180, record a mandatory `on-track` or `handoff-required` forecast.
- Derive a pessimistic `latestEffectStartAt` and an earlier `runnerHardDeadlineAt` from action duration plus cancellation/rollback reserve.
- Enforce `assignmentStartedAt < latestEffectStartAt <= runnerHardDeadlineAt < settlementCutoffAt < assignmentDeadlineAt`.
- By T+210, every process and effect must be verified settled, verified rolled back, or formally fail-stuck behind the retained operation latch. Fail-stuck is Recovery, never success.
- T+210 through T+240 is only for verification, durable receipts, credential/lease release, and handoff.
- At T+240, the identity loses all effect authority. No renewal, reconnect, rename, restart, or new TaskRun iteration resets the clock or cumulative budget.

### Handoff

If safe completion is not highly credible by T+210, stop new work and prepare handoff. Freeze exact repository and external state, then record `AssignmentHandoffPrepared` with assignment lineage, elapsed time, completed acceptance items, tests, evidence, mutations, recovery-latch state, authority disposition, and exactly one next permitted action.

Create one fresh successor using the same reservation/activation/context-acceptance protocol. The successor remains read-only while independently verifying the context, receipts, worktree, and external-state fingerprints. Only `AssignmentHandoffAccepted` transfers effect authority and closes the old identity. The old agent is read-only from preparation onward.

Each immutable slice may use at most one successor assignment under any label. If that successor also forecasts overrun, record `AssignmentRescopeRequired`, stop the slice, and charter smaller reviewed slice IDs in a linked TaskRun iteration. Never create a relay chain or another Jira ticket. Cumulative assignment count, agent-minutes, calendar time, and acceptance obligations never reset.

### Default task budgets

For each canonical task, charter lower values where sufficient and never exceed without a prior direct Product Owner decision plus distinct Product and Project/Delivery Council approval:

- six planned work slices;
- 24 total agent assignments;
- 5,760 summed agent-minutes;
- 10,080 calendar minutes, including waits and gaps;
- one successor assignment per slice;
- five newly active worktrees; and
- one repeated semantic repair class before architecture reset.

At least half of effort must produce executable or acceptance evidence rather than governance scaffolding. A budget breach is an owner-visible rebaseline, not authority to add an agent, slice, PR, or task.

## TaskRun coordinator and generation-0 bootstrap

Before any ordinary TaskRun, Jira mutation, private action, deployment, or delivery-status change, implement, normally merge, and independently verify the complete assignment-control foundation defined by the pinned operating model:

- an immutable context envelope;
- append-only hash-chained TaskRun events and deterministic state reducer;
- a project-level atomic compare-and-set assignment coordinator with monotonically increasing fencing tokens;
- assignment-aware admission and serializable runner;
- platform identity/context attestation;
- deadline enforcement and complete process-tree cancellation;
- an assignment-independent external-operation recovery latch; and
- the evidence-gated delivery-transition mechanism.

TaskRun state is non-authorizing. It cannot change requirements, Gate A/B, owner actions, scope, delivery status, or `executionAllowed`.

Generation 0 is the sole bootstrap exception. It is limited to its Goal-owned heartbeat control, coordinator/runner source, local tests, a scoped branch, a normal reviewed PR/merge, and exact-main generation-1 genesis. It cannot create Jira work, execute an ordinary delivery task, access a private system, deploy, change delivery status, or touch R1-R10.

This activated Goal deliberately and narrowly supersedes the pinned operating model's otherwise circular generation-0 requirement for a pre-existing separate CAS store, watchdog/tool broker, recovery latch, and reservation of the already-created root identity. The current-platform alternative is:

1. The direct Codex Goal record supplies root genesis. Read `get_goal` before work and bind the platform Goal/task identity, creation or elapsed-time origin, this Goal digest, operating-model digest, exact source revision, and authority ceiling in the first generation-0 receipt. Do not pretend the already-created root was reserved or spawned by itself.
2. The persistent root supervisor is the sole integration writer and sole actor permitted to perform generation-0 branch, push, pull-request, expected-head merge, and genesis effects. It performs no product implementation. Each material supervisor action is a separately recorded slice with its own start, forecast, effect cutoff, rollback boundary, and end inside 240 minutes.
3. Every spawned generation-0 worker, verifier, Council reviewer, or research helper uses `AssignmentReserved -> AssignmentActivated -> AssignmentContextAccepted`, a fresh platform agent ID, `fork_turns=none`, an isolated worktree where needed, and the normal T+180/T+210/T+240 limits. Give those agents no Jira, GitHub-write, private-system, credential, deployment, or delivery-transition authority.
4. Create a Goal-owned Codex heartbeat automation before the first child spawn using the app's `codex_app__automation_update` capability, locating it through the enabled-tool inventory if it is lazily exposed. Do not write a raw schedule directive. At bounded intervals the heartbeat rereads Goal elapsed time and the assignment ledger, requires the T+180 forecast, and tells the active supervisor to interrupt an expired or settlement-cutoff child through the platform interrupt control. Delete that automation at generation-1 settlement, pause, or terminal. A shell sleep is never the watchdog.
5. Until the durable coordinator exists, use the direct Goal plus an append-only generation-0 receipt in the reviewed source/PR record as the bootstrap ledger. The root serializes every effect, records preimage and expected remote head, uses an explicit non-`main` refspec, performs one bounded call, rereads the remote/PR result, and retains a recovery-required state on any uncertain outcome. Do not delegate or concurrently issue an external effect.

This source-bootstrap exception is a consciously narrower current-platform mechanism, not a claim that Codex already provides the generation-1 broker. It permits only the exact coordinator/runner implementation, local tests, scoped `codex/*` branch, explicit non-`main` push, reviewed PR, expected-head normal merge, and exact-main generation-1 genesis already allowed above. It permits no ordinary TaskRun, Jira call, private-system access, deployment, delivery transition, authentic content, or R1-R10 work. If `get_goal`, platform agent IDs, fresh isolated agents/worktrees, agent interruption, the Goal-owned heartbeat, or exact expected-head Git verification is unavailable, report that exact missing primitive and keep ordinary lanes closed.

Generation 0 must still satisfy the immutable source scope, three-phase child-assignment start, finite assignment/agent-minute/calendar/PR ceilings, independent verification, and irreversible generation-0 tombstone. Its purpose is to implement and prove the durable atomic coordinator, assignment-aware runner, recovery latch, and enforcement needed from generation 1 onward. Immediately after verified generation-1 genesis, expire this current-platform exception permanently; every later action is subject to the full coordinator, deadline, fencing, and recovery-latch model.

## Jira projection contract

Jira is a conditional downstream projection, never the source of scope, acceptance, delivery state, or execution authority. The repository currently has no approved personal Jira site, project, connector, schema, identity field, field map, or mutation authority. Do not invoke the installed Toast Jira setup/create/update flows for this personal project.

Use an owner-controlled, validator-backed decision with exactly `not-adopted`, `pilot-authorized`, or `adopted`:

- Start or remain `not-adopted` unless a direct owner decision says otherwise.
- Only `SPK-R0-001` may be `pilot-authorized`.
- A pilot must become `adopted` or `not-adopted` before the task closes.
- When not adopted or unconfigured, record `Jira projection pending — bootstrap authority/configuration absent`, make no Jira call, and make no synchronization claim. This does not block safe local protocol proof.

Before the first Jira write, require the owner-approved target and project type/key, credential broker, account/permissions, hierarchy, parent, milestones/Fix Versions, workflow/transition/resolution map, field/option IDs, immutable Task ID property, account mapping, public/private key-and-link policy, duplicate/archive/cancellation policy, connector/adapter, dry-run/apply/verify contract, atomic Jira lease, and privacy/recovery tests.

After adoption, map at most one Jira issue to each authorized canonical Task ID using immutable identity `life-reflection:phase1:<TASK-ID>`. A worker, verifier, Council seat, TaskRun, slice, handoff, or retry never gets another delivery ticket. Preserve unmanaged human fields and keep Jira workflow/resolution separate from canonical Roadmap Status.

Every write is supervisor-fenced: recompute the managed payload from exact merged main, search by immutable Task ID, reconcile the durable public-safe map or private opaque broker, refuse ambiguity/duplicates, produce a field-level dry-run, mutate once through proved CAS/version semantics or a single bounded pre-read/write/post-read sequence, and verify normalized parity. Never blind-retry or create a replacement ticket after a timeout. An uncertain outcome retains the assignment-independent recovery latch and permits only read-only reconciliation until separate recovery admission.

The Jira lease must expire no later than the assignment runner hard deadline. A successor needs a higher fencing token bound to its accepted assignment digest; a higher lease token never clears an uncertain-operation latch.

## Gate model and per-task lifecycle

Use one task cell at a time:

1. **Select:** choose the next dependency-valid existing P0/R0 task.
2. **Charter:** create the immutable TaskRun, authority ceiling, exact Definition of Done, verifier oracle input, rollback boundary, and finite four-hour slice plan.
3. **Jira disposition:** create/reconcile only when adopted or pilot-authorized for SPK; otherwise record the explicit non-adoption/bootstrap-pending result.
4. **Verifier first:** have a fresh verifier author oracle v1 and the protocol-DAG challenge before substantive work.
5. **Proposal:** use a proposal-only worker to prepare the task's Product, Architecture, Design, QA, Delivery, and Council artifacts.
6. **Gate A:** obtain five distinct proposal verdicts bound to one immutable proposal. Gate A permits only named local/public/fictional/synthetic preparation and always leaves `executionAllowed=false`.
7. **Build:** use a fresh worker to implement only the accepted bounded slices.
8. **Freeze:** the supervisor verifies lineage, allowed paths, and digests, then freezes one exact candidate SHA. Worker mutation stops.
9. **Independent QA:** the verifier reproduces the oracle against the exact candidate and returns Pass or Hold. Hold creates a linked fresh iteration; the supervisor cannot override it.
10. **Exact-candidate Council and CI:** obtain five distinct exact-candidate verdicts and all current required checks.
11. **Integrate:** use a scoped reviewed PR and normal merge. A merge is not Gate B, execution, deployment, or acceptance.
12. **Gate B:** publish one immutable task/stage/scope/action/candidate/assignment-bound approval with current dependencies, owner actions, rollback, five seats, deadline, idempotency, module identity, and explicit `executionAllowed=true` for that exact action.
13. **Guarded action:** diagnostic admission is necessary but not an authorization token. Perform a governed read, mutation, migration, deployment, restore, rollback, or acceptance action only inside the reviewed assignment-aware exact-main serializable runner. Revalidate exact main, Gate B, predecessor, assignment digest, fencing token, deadlines, owner actions, and recovery latch immediately before effect. Accept the receipt only after process settlement and outcome verification.
14. **Post-effect verification:** a fresh verifier confirms deployed/build identity, health, privacy/security, migration, restart, backup, separate-path restore, rollback, non-regression, accessibility, and named task evidence where applicable.
15. **Acceptance publication:** publish only sanitized executed evidence and authoritative status inputs through the reviewed non-self-referential topology.
16. **Delivery transition:** every status edge uses its own task-bound stage ending `-DELIVERY-TRANSITION`, Gate A, immutable Gate B, `delivery-control/delivery-status-transition`, `p0.delivery-transition`, guarded runner, rollback snapshot, and settled verification. Generic sync, Jira workflow, issue closure, a PR, or Council verdict cannot move status.
17. **Living projections:** regenerate and reconcile source, manifest, Markdown plan, GitHub, Excel, Wiki, `RUNNING_LOG.md`, and Jira only when adopted.
18. **Close:** revoke credentials/leases, stop Goal-owned schedules, classify worktrees, close every worker/verifier/Jira/Council agent after durable receipts, prove assignments and latches settled, then select the next dependency-valid task.

Do not assume the simplified four-phase publication topology already exists. Current arm/consume ratchets remain controlling until a separately reviewed successor change, validators, rollback proof, AGENTS update, and exact-main checks accept the simpler topology. If simplification cannot be established within the approved budget, stop for owner-visible rebaseline rather than silently returning to recursive ceremony.

## Continuation sequence from the pause

### Stage A — resume, simplify, and make the operating model executable

1. Refresh and reconcile the exact pause state without invoking the v1 candidate or touching preserved worktrees.
2. Treat this directly submitted Goal as the owner-bound **simplify before salvage** decision. Bind it in the immutable generation-0 bootstrap and durable PR/check record; append the dated source decision only after generation 1 exists and the running-log/source path is lawfully available.
3. Implement and independently verify generation 0, the assignment coordinator, TaskRun ledger/reducer, assignment-aware runner, recovery latch, and generation-1 genesis. This is the only mutation path before the coordinator exists.
4. After generation 1 exists, reconcile stale pause/active wording in `AGENTS.md`, README, INDEX, and the living projections through a reviewed ordinary source PR; do not make a task-status change merely because execution resumed.
5. Through a successor-control TaskRun anchored to existing issue #3/`PC-001`, not a new canonical issue and not a rewrite of its historical record, independently prove and enable the currently disabled delivery-transition apply path. Mechanism enablement grants no task-edge authority.
6. Rebaseline the current proposal/preparation/stage arm-consume topology into the smallest independently proven non-self-referential publication path that preserves every real safety property. Update validators, CI, AGENTS, rollback proof, and exact-main tests together.
7. Add protocol-DAG, cross-document invariant, progress/timing, flake, and budget checks before another frozen candidate.
8. Decide whether Jira remains `not-adopted`, is authorized for the exact SPK pilot, or has completed adoption. Do not invent the decision.
9. Pass all Stage A controls twice from clean exact main with independent/adversarial no-veto review before opening an ordinary task cell.

### Stage B — repair and complete `SPK-R0-001`

Do not run the merged v1 candidate-QA contract and do not reuse its candidate identity or revision-derived evidence.

1. Reduce the candidate-QA protocol to the smallest truthful evidence set that proves the required synthetic scenarios.
2. Draw the complete acyclic writer/reader/finalization/digest graph.
3. Separate current public dependency acquisition from no-egress local execution and attestation.
4. Freeze the complete command list, order, repetitions, isolation mechanism, artifacts, and digest subjects.
5. Build one disposable fictional reference producer.
6. Have a separately implemented black-box verifier reconstruct and accept the bundle from the contract alone.
7. Run both complete paths twice before protected publication.
8. Use a focused repair wave under the current or successfully rebaselined controls.
9. If candidate QA has not run after three additional source PRs, stop and request an owner-visible strategy rebaseline instead of adding more ceremony.
10. After candidate QA, Independent QA, Council, Gate B, governed private stages, and exact acceptance evidence pass, transition `SPK-R0-001` through its reviewed status stage.

Private capacity, topology, collision, restart, backup/restore, rollback, and co-resident non-regression evidence requires current exact private authority. If absent, complete local proof and hold only the private lane.

### Stage C — complete R0 in dependency order

1. Preserve `AUD-001`, `PC-001`, and `PRD-R0-001` as historical non-authorizing planning/audit `Done`; verify their named evidence but never create execution stages for them.
2. Complete `UX-R0-001` in its own fresh task cell with normal, empty, loading, error, interruption, retry, destructive, responsive, accessibility, and prototype-disposition evidence.
3. Complete `ARCH-R0-001` in its own later task cell with the accepted threat model, interfaces, data shapes, secrets/log/cache boundaries, capacity, migration, encrypted backup, separate-path restore, rollback, and coexistence design evidence.
4. Complete `ENG-R0-001` only after UX and Architecture gates. Implement the synthetic private shell, tests, immutable build and SBOM, reversible migration, access denial, evidence-safe observability, authorized synthetic deployment, restart, encrypted backup, separate-path restore, rollback, and non-regression evidence.
5. Complete `REL-R0-001` with independent QA and five-seat release review covering access/denial, callback isolation, secret scan, authenticated ciphertext and wrong-key behavior, cache/log safety, truthful health, capacity, restart, encrypted backup, separate-path restore, representative synthetic decrypt, two off-server synthetic key-location classes described without secret locations, measured recovery against the R0 four-hour target, rollback, accessibility, privacy, and zero unresolved severity-1/2 or critical/high security/privacy findings.

Every one of the 11 R0 requirements needs requirement-level executed R0 evidence. A backup upload is not restore evidence. `REL-R0-001` must record `Proceed`; `Hold` or `Rollback` keeps the Goal incomplete.

## Human-only, private, and privacy boundaries

No agent may impersonate account/MFA action, secret provisioning, private-target authority, provider terms, material spend, authentic-content UAT, recovery-key custody, Recovery Ceremony, or later launch acceptance.

Before any private host/provider/tunnel/DNS/backup/target read or mutation, require valid exact-scope `P0-OA-001` plus the applicable R0 owner/action record and private evidence custody. Require `R0-OA-001` for MFA/authentication/credential provisioning and `R0-OA-002` only when a material provider, terms, paid account, or recurring cost is actually implicated. `P0-OA-002` is due only for an otherwise separately authorized Project-workflow or non-delivery mutation. Do not request or change R1-R10 owner actions.

Never ask for a secret in chat or search unrelated keychains, browser storage, shell history, repositories, or backups. Private commands must run only through the approved runner that captures raw stdout/stderr directly into approved access-controlled custody and returns a fixed sanitized schema. On capture/sanitizer failure, expose no raw tail, path, target, account, or value.

Use fictional/synthetic memory fixtures only. No authentic journal, photo, photo-derived description, audio, transcript, or personal-memory data may enter any agent, test, R0 target, artifact, or evidence path. The only narrow real-data exception is the minimum live authentication assertion processed solely by the authorized R0 target for bounded access/denial proof under valid authority; its value must never reach Codex, logs, fixtures, public evidence, or persistence.

## Living-plan and publication contract

Evidence and governing source change first. Then regenerate and reconcile in the same accepted change:

1. task Product/Architecture/Design/QA/Delivery/Council artifacts and decisions;
2. roadmap source, task-artifact inputs, manifest, and Markdown release plan;
3. the existing 58 GitHub issues and Project fields/views when exact mutation authority exists;
4. the public issue map where identity legitimately changes;
5. the canonical Excel release-plan workbook using the installed `spreadsheets:Spreadsheets` skill and whole-workbook semantic/render oracle;
6. the Wiki generated from exact merged main with one-to-one Page Audit and no broken links/fragments;
7. Jira only when adopted; and
8. the append-only running log or the non-self-referential durable PR/check evidence channel when a frozen candidate forbids a source append.

Use only `Backlog`, `Next`, `In progress`, and `Done` for canonical delivery status. Keep delivery status, artifact readiness, execution decision, execution permission, issue state, Jira workflow/resolution, agent-run state, stage state, and deployment state distinct. Evidence precedes every status transition.

External writes are not transactional. Use dry-run, preimage/rollback capture, stable identity, one fenced writer, read-after-write verification, and immediate plus quiescent reconciliation. Never manually edit a projection to conceal source drift. Same-build workbook copies must be byte-identical; an isolated later rebuild is compared by complete sheet/order/used-range/cell/formula/link/count/R10/render semantics, not OOXML packaging bytes.

Do not create a source/Wiki/log self-reference loop. Put postmerge and external-settlement evidence in the existing durable PR/check record when another repository edit would recursively require another publication wave; append its public-safe recap in the next ordinary pre-candidate source wave.

## Mandatory running-log cadence

During generation 0, do not append `RUNNING_LOG.md`; retain bootstrap events in the immutable bootstrap store and durable PR/check record. After generation 1 is verified and the reviewed source/log path is lawfully available, invoke `codex-project-running-log`, read the existing header and latest trusted entry, and append directly without requesting a second confirmation under this Goal's explicit standing authorization. Never rewrite prior bytes.

Append a public-safe entry:

- at generation-1 supervisor bootstrap and every later supervisor handoff;
- at each TaskRun start and closure;
- after a material scope, dependency, authority, candidate, Gate, status, blocker, recovery, or rollback change;
- before and after an authorized external mutation or deployment, subject to immutable-candidate publication rules;
- after failures and accepted handoffs;
- at least every 45 minutes when material state has changed;
- before context compaction or long handoff; and
- before the terminal publication wave.

Once candidate bytes freeze, use TaskRun, PR, check, and approval records for post-freeze events that cannot lawfully become candidate descendants. The next ordinary pre-candidate source wave appends a recap. At terminal, the final PR/check summary is the append-equivalent for postmerge projection results; do not create an infinite terminal-log/source/Wiki loop.

Every entry states current branch/revision, TaskRun/assignment IDs, worktree classification, PR/deployment/test/evidence results, authority, blockers, next permitted action, and what was not claimed. Never log secrets, private topology, authentic content, raw provider output, private Jira keys/URLs, or private Project node IDs.

## Failure, recovery, and pause rules

- Any ambiguity in source, target, identity, authority, assignment ownership, deadline, evidence, credential, rollback, or external outcome is Hold.
- Never lower a gate or redefine acceptance after a failure.
- Any uncertain external effect retains its assignment-independent conflict-domain recovery latch across agent, stage, lease, credential, and supervisor expiry.
- A successor may only read/reconcile until an exact higher-generation `RecoveryAdmitted` transition binds the retained latch and recovery Gate B.
- A PR merge, Jira resolution, issue closure, green CI result, prototype, code artifact, backup upload, or healthy endpoint is not sufficient completion evidence.
- If a human-only action blocks one lane, complete every safe in-scope lane, produce one minimum-action checklist with exact gate and safe input channel, close the active assignment, and leave the Goal incomplete.
- If the same semantic repair class recurs, stop incrementally patching and perform the required architecture rebaseline.
- Never use time pressure or the four-hour lease to bypass privacy, verification, rollback, Council, or Gate requirements.

## Definition of Done

Do not mark this Goal complete until all conditions below are directly evidenced.

### Per-task closure

- The task's named acceptance evidence exists and is linked.
- For each of the five substantive R0 tasks, the exact candidate passed independent verification and every applicable five-seat, CI, Gate A, Gate B, guarded-runner, rollback, and post-effect check.
- Required implementation is on merged main and any required deployment/restore/rollback evidence binds the exact build and target envelope.
- No unresolved Sev-1/Sev-2 or critical/high security/privacy finding remains.
- For each of the five substantive R0 tasks, the exact task-bound delivery-transition stage applied and verified every status edge created by this Goal. The three historical planning/audit tasks receive no execution or delivery-transition stage.
- GitHub, manifest, Excel, Wiki, running log, and adopted Jira agree immediately and after quiescence; when Jira is not adopted, its explicit disposition is truthful and no synchronization is claimed.
- Every assignment stayed within its nonrenewable lease or transferred through one accepted fresh successor; all cumulative budgets remain within the charter.
- Credentials, leases, schedules, workers, verifiers, Jira managers, and Council agents are closed or revoked, recovery latches are settled, and worktrees are classified.

### Bounded P0/R0 terminal

- All eight in-scope roadmap tasks are honestly `Done`: the five substantive R0 tasks through reviewed transitions created by this Goal, and the three historical planning/audit tasks through their preserved prior evidence only.
- `AUD-001`, `PC-001`, and `PRD-R0-001` retain `Historical non-authorizing` meaning and receive no new execution or delivery-transition stage.
- `SPK-R0-001`, `UX-R0-001`, `ARCH-R0-001`, `ENG-R0-001`, and `REL-R0-001` have their named executed evidence and no-veto independent acceptance.
- All 11 R0 requirements have requirement-level executed R0 evidence.
- `REL-R0-001` records `Proceed`.
- No authentic memory or photo entered the path.
- Every due P0/R0 owner action was satisfied only at its exact gate.
- All 50 R1-R10 tasks and 300 artifacts remain frozen and execution-disallowed.
- Every completed stage is terminal and non-replayable; all eight in-scope tasks end with `executionAllowed=false`, with approvals retained only as evidence.
- No temporary agent, credential, lease, lock, schedule, process, recovery latch, or Goal-scoped mutation permission remains active.
- Manifest, Markdown plan, all 58 issues, milestones, 17 managed Project fields, delivery views, issue map, workbook, Wiki/Page Audit, running log, and adopted Jira reconcile with zero unexplained mismatch.
- All public links resolve from remote main; final immediate and quiescent verifiers pass; public-safety scans are clean.
- The exact terminal statement is durably published:

> **Bounded Codex Goal COMPLETE: P0 control baseline and R0 synthetic private foundation accepted. Phase 1 remains incomplete; R1-R10 are out of scope and were not started by this Goal.**

After that statement is verified, mark this Codex Goal complete and stop immediately. Do not select, prepare, recommend, or create an R1 successor inside this Goal.

If any condition is missing, the Goal remains incomplete. Never mark the Goal complete because time or token budget is low.

## First actions after valid activation

1. Verify the publication prerequisite, exact operating-model digest, owner-selected worktree identity, clean exact-main state, and canonical remote without exposing sensitive values.
2. Read and attest every mandatory source; refresh live repository/GitHub/Project/Wiki/workbook/owner-action/process/worktree state.
3. Snapshot the 50 frozen tasks, the exact 58 canonical Phase 1 issue identities, and the unrelated-Project-item exclusion set; classify preserved worktrees read-only.
4. Bind **simplify before salvage** from this direct Goal and build the current-state/authority/complexity baseline without a source-log append.
5. Charter the finite generation-0 bootstrap with assignment, agent-minute, calendar, worktree, and source-PR ceilings, then create its Goal-owned heartbeat before the first child spawn.
6. Create the fresh independent verifier and required Council waves under the reservation/activation/context-acceptance protocol.
7. Implement, review, normally merge, and verify the assignment coordinator, TaskRun ledger, assignment-aware runner, recovery latch, and generation-1 genesis without starting an ordinary task or globally enabling delivery transitions.
8. After generation 1 exists, invoke the running-log skill and append the bounded resume plus generation-0 closure entry, preserving every prior byte.
9. Reconcile pause/authority language and living projections through the accepted publication path.
10. Through the post-generation-1 successor-control TaskRun anchored to `PC-001`, prove and enable the delivery-transition apply mechanism without granting any task-edge authority.
11. Decide and record the Jira disposition without making an unauthorized Jira call.
12. Open the first ordinary task cell for `SPK-R0-001`, prove the minimal disposable producer/verifier vertical slice, and continue through the lifecycle above.
13. Complete `SPK-R0-001`, then `UX-R0-001`, `ARCH-R0-001`, `ENG-R0-001`, and `REL-R0-001` sequentially.
14. Perform bounded terminal reconciliation, publish the exact terminal statement, mark the Goal complete, and stop before R1.
