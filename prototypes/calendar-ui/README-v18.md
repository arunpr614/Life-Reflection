# Life in Days prototype v18 — History and Provenance

> **Unfrozen implementation candidate under same-version evidence repair · independent QA Round 1 FAIL — C0/H0/M3/L0 · fresh independent QA pending · fictional deterministic data · browser memory only · no backend, VoiceNotes, persistence, provider, security, deployment, accessibility-conformance, or production claim**

V18 is an additive, read-only History and Provenance capsule over the frozen v16 archive and the frozen v17 compatibility runtime. It represents separate Source and Derived history, exact record lineage, six truthful entry origins, safe provenance, filtering, and deterministic loading branches without changing an archive record.

Candidate-owner checks are not independent QA and do not close a requirement. Until the held candidate passes fresh QA, freeze, explicit push, and remote readback, program arithmetic remains **19/57 prototype-representable rows closed and 38/57 open**.

The earlier six-byte candidate recorded in `PRE-GATE-FAIL-v18.md` is rejected: it made unrelated 2 Aug, Before sleep, generated-field, and artwork controls appear to own the fixed 17 Aug / Monsoon walk history, and three returns exceeded the one-pixel tolerance. C18-21 authorizes only the canonical-entry repair below; C18-22 authorizes only the staged responsive environment used to prove both native Global origins in frame 16. Neither decision accepts that fingerprint or its producer-local captures.

The three bounded primary targets are `LID-SCP-003`, `LID-VN-006`, and `LID-REF-004`. `LID-SRC-004` is supporting inherited regression only and is not counted again. `LID-VN-005` remains external evidence, and no `LID-HIS-*` identifier exists.

## Round 1 visual-evidence repair contract

Independent QA Round 1 rejected the held evidence at **C0/H0/M3/L0**. The obsolete images remain historical evidence and cannot be reused for freeze. The same-version repair keeps exactly 17 events, 14 fixtures, two disjoint capture scenarios, 16 PNG/JSON pairs, and the exact 16 CLI commands below; it changes no fixture, scenario, viewport, media, or filename identity.

- Frames 02, 03, 05–13, and 15 use an exact closed per-frame pre-capture framing map. The helper may perform only one bounded passive scroll after final fixture state and before the screenshot. It resolves named proof targets, verifies their required safe copy without exporting protected source prose, proves their union fits the requested viewport, and then records each target's rectangle, full visibility, fixed/sticky-overlap result, unchanged snapshot/focus, scroll geometry, and fail-closed assertions under `capture.framing`. At medium width only, expanded E12 uses whitespace-only compaction with unchanged text, order, semantics, targets, and focus behavior; both section headings retain at least 12 px separation.
- The target map exposes the previously missing proof: Day `Current source context` → `Correction 1 · Displayed`, `Latest event-time Journal Date` → `18 Aug 2026 → 17 Aug 2026`, and unchanged current Journal Date/lane counts; Item conflict and both expanded lineage groups; Artwork V2 Historical/Stale/AI label; upstream-revised E04-only `Retained Source Revisions · Revision 1 · Revision 2` and the external-evidence boundary; Untagged retained-local fact; Deleted-upstream local-live fact; hidden-day banner plus separate `Source lane` → `2 retained events` and `Derived lane` → `1 retained event` rows; interrupted retained-list/copy; contextual failure retry; and canonical top-level empty copy plus its one in-panel **Back to Settings** action.
- Frame 10 applies the same passive framing only after a real reverse-keyboard path reopens **Filter history · 2 active**: native Shift+Tab traversal reaches the summary, Enter activates it, the summary remains focused, and `:focus-visible` plus a rendered outline or shadow must pass before capture. Direct focus and pointer reopening are rejected for the final step.
- Frame 11 binds only the exact `loading` fixture at 568×320 with no scenario. After settle, one bounded final scroll changes only `scrollY` and places the complete named Source lane boundary, `h2#lid-v18-loading-source-title` **Source history**, `h3#lid-v18-loading-title` **Loading history**, and the exact loading body inside the 52 px top / 16 px bottom safe band. The Source lane is the sole `aria-busy="true"` node. The helper records target rectangles, overlap results, busy/heading/text cardinality, and preserved DOM/CSS/ARIA/viewport/media/action digest; focus remains on the intentionally offscreen History h1, with no visible-focus claim for this frame.
- Frame 14 keeps the exact five-record scenario and app-owned one-pixel anchors. Because the final Source and Derived beginning markers are roughly 2,900 document pixels apart in a 900 px viewport, the product renders one normal-flow, read-only completion summary after both lane lists. It truthfully shows Source `10 shown · Exactly 3 earlier added · Beginning reached` and Derived `7 shown · Exactly 3 earlier added · Beginning reached`; the focused Derived beginning marker remains adjacent and visibly outlined. The helper neither moves focus nor compensates after either governed Load baseline.
- Frame 16 adds normal-flow compact bottom safe-area spacing. Before the final Artwork activation, the helper performs its only allowed pre-baseline scroll and requires at least 12 px between both the future focus ring and panel bottom boundary and the fixed compact navigation. After the app-owned return, it waits passively for the native 4.2-second toast timer to expire, preserving state, focus, and scroll. Final capture fails closed unless the toast is absent; the complete H2 and description remain below the fixed banner; all four facts and buttons, the Artwork focus ring, and the complete panel bottom boundary remain visible; and both navigation clearances remain at least 12 px.

These are candidate-owner repair mechanics, not an independent-QA pass. Every evidence pair must be recaptured after the final repaired helper/application/style bytes, sealed in a replacement manifest, and assigned to a fresh read-only QA agent from zero.

## Post-QA2 repair contract

The post-QA2 repair remains an unfrozen same-version candidate. It changes only `app-v18.js`, `styles-v18.css`, `capture-phase2-evidence-v18.mjs`, this runbook, and `check-v18.mjs`; it preserves exactly 17 events, 14 fixtures, two capture scenarios, 16 frames, six additive assets, and the privacy/read-only boundary.

- A terminal pagination outcome—success, failure, interruption, or duplicate, including every generic and settle alias—requires an explicit positive integer generation for the exact same lane. Missing, zero, negative, fractional, string, conflicting-alias, stale, future, cross-lane, wrong-stage, and already-consumed generations are strict no-ops: no state, focus, announcement, anchor, digest, counter, DOM, or navigation change. The visible prototype-only success/failure/interruption buttons carry the current request generation; the duplicate button carries exactly the next generation. The frame-14 scenario supplies the exact live generation for both QA success records while retaining its original five-record transcript.
- A click-only Load or Duplicate activation receives an app-owned anchor fallback on window capture only when no pointerdown or keydown precursor baseline exists. Pointer, Enter, Space, and click-only paths must each create one generation, retain the logical lane control, and restore its focus and top within 1 CSS pixel; click-only duplicate must consume exactly the next generation. The helper performs no post-baseline focus or scroll repair and makes no assistive-technology claim.
- The normal-flow pagination completion summary is suppressed whenever any applied filter is active. Source plus Needs attention must render exactly `E14,E13,E12`, total 3, with no completion summary in DOM or accessibility-tree inspection. Clearing filters restores the truthful unfiltered Source 10 / Derived 7 summary without delivery, reset, generation, or anchor drift.
- Origin interception is fail-closed before `preventDefault`, propagation suppression, return-anchor capture, feature open, focus, scroll, announcement, or domain state. The exact button must be connected, enabled, rendered, non-hidden, non-inert, outside every `aria-hidden="true"` ancestor, non-zero, viewport-visible, centre-hit-testable, and have a non-empty accessible-name proxy on the correct responsive root. Global More is eligible through 960 px; Global Settings starts at 961 px. This origin breakpoint is independent of the unchanged filter-layout breakpoint, which remains compact through 1023 px and wide from 1024 px. Live diagnostics require More positive / Settings negative at 960, Settings positive / More negative at 961 and 1023, and retain More positive / Settings negative at 320 plus Settings positive / More negative at 1024. Every positive return restores the same invoker within 1 CSS pixel. Negative probes observe the post-V18 document-capture boundary without preventing or suppressing the frozen v16 handler; any later frozen toast/modal behavior is recorded separately and passively settled.
- At 320×900 forced colours, the fixed banner bottom is 66 px and compact-navigation top is 834 px. The complete canonical panel must be at most 744 px high, top at least 78 px, bottom at most 822 px, with its top and bottom borders, eyebrow, H2, body, four facts, four buttons, and the Artwork 3 px focus ring all visible. The layout retains the exact 32 px gutter, readable unshrunk text, at least 44 px controls, normal flow, and at least 12 px clearance from both fixed surfaces; truncation, nested scrolling, overlay positioning, or post-baseline helper compensation fails closed.

These checks are candidate-owner regressions and do not claim independent QA, freeze, backend behavior, security, accessibility conformance, deployment, or production readiness.

## Run

From this directory:

```sh
node --check app-v18.js
node --check capture-phase2-evidence-v18.mjs
node --check check-v18.mjs
node check-v18.mjs
LIFE_IN_DAYS_PROTOTYPE_PORT=4317 node serve.mjs
```

Open `http://127.0.0.1:4317/index-v18.html` without a query or hash.

Fresh load leaves every capsule inactive and shows the inherited v16 archive followed by the canonical v18 entry panel. Global History is reachable only through the unchanged native **History** action in Settings or compact More. Contextual History is reachable only through the four owned controls in that panel. The frozen runtime launcher is a retired compatibility node: on v18 it is hidden, disabled, `aria-hidden="true"`, `tabindex="-1"`, computed `display:none`, non-hit-testable, absent from the accessibility tree, and unable to open v18 or receive fallback focus. The capsule does not use browser history, URL state, or storage for private scope.

## Additive architecture

The load order is deliberate:

1. frozen v16 styles and `app-v16.js` render the inherited archive;
2. frozen `styles-v17.css`, `runtime-v17.js`, and `app-v17.js` register the shared compatibility host and Atomic Redating capsule; and
3. `styles-v18.css` and `app-v18.js` register the History and Provenance capsule as the latest compatible feature.

`runtime-v17.js` remains the only owner of `window.__LID_QA__`. On `index-v18.html`, its detached manifest must report target version `18`, loaded versions `[17, 18]`, and v18 as the active QA feature. Direct `index-v17.html` behavior remains frozen and independently testable.

No v1–v17 file, `package.json`, `serve.mjs`, external dependency, shared latest file, font, network service, or production-looking data source is added or changed by v18. The additive v18 roster is exactly:

| File | Role |
| --- | --- |
| `index-v18.html` | Frozen v16/v17 dependency chain plus v18 assets |
| `app-v18.js` | History fixtures, transitions, rendering, detached snapshot, and invariants |
| `styles-v18.css` | V18-scoped responsive, focus, theme, reduced-motion, and forced-colour presentation |
| `README-v18.md` | This bounded runbook |
| `check-v18.mjs` | Frozen-byte, package, semantic, and privacy/static validator |
| `capture-phase2-evidence-v18.mjs` | Scenario-aware v18 Chrome/DevTools evidence driver using Node built-ins only |

## Exact represented contract

The accepted normal corpus is exactly `E01`–`E17`. Approach C renders two separate newest-first ordered lists in one page scroll:

- **Source history:** `E10,E14,E13,E12,E11,E05,E04,E01,E17,E15`
- **Derived history:** `E09,E08,E07,E06,E03,E02,E16`

The canonical cross-lane order remains detached snapshot and QA truth only: `E10,E14,E13,E12,E11,E09,E08,E07,E06,E05,E04,E03,E02,E01,E17,E16,E15`. It is never rendered as a merged third feed.

### Truthful entry boundary

Global History has exactly two native origins: Settings **History** and compact More **History**. V18 evaluates only `[data-action="settings-related"][data-label="History"]` at activation time in a document capture-phase listener. More owns widths at or below 960 px; Settings owns widths at or above 961 px. The distinct filter presentation remains compact below 1024 px and wide at 1024 px and above. Before any event suppression or state effect, the exact invoker must pass the connected, enabled, rendered, non-hidden, non-inert, non-`aria-hidden`, non-zero, viewport, centre-hit, accessible-name-proxy, responsive-surface, and root-containment checks. The controls keep their native text and attributes, no observer decorates them, and eligible invokers open fresh `global-ready` with **Back to Settings** or **Back to More**. Ineligible invokers are V18 no-ops and remain available to the frozen native handler.

There is exactly one owned contextual section:

```html
<section id="lid-v18-canonical-entry-panel"
         class="lid-v18-canonical-entry-panel"
         aria-labelledby="lid-v18-canonical-entry-title"
         aria-describedby="lid-v18-canonical-entry-description">
```

Its exact visible introduction is:

- eyebrow: **PROTOTYPE V18 · FIXED SYNTHETIC CONTEXTS**;
- heading: **Open canonical History contexts**; and
- body: **These controls open the fixed fictional 17 Aug 2026 history for Monsoon walk note. They do not represent the Journal Days, Source Items, generated fields, or artwork shown elsewhere in the frozen v16 archive.**

One `<ol>` contains exactly these four entries in order. Every visible fact has a stable ID, and the adjacent button's `aria-describedby` references that fact while its accessible name remains the exact visible label.

| Visible fact | Button ID and owned token | Exact button / fresh scope / return |
| --- | --- | --- |
| **Journal Day · 17 Aug 2026** | `lid-v18-canonical-entry-day` / `data-lid-v18-canonical-entry="day"` | **History & provenance** / `day-ready`, 14 events / **Back to Journal Day** |
| **Source Item · Monsoon walk note** | `lid-v18-canonical-entry-item` / `data-lid-v18-canonical-entry="item"` | **View source history** / `item-ready`, 14 events / **Back to Source Item** |
| **Generated field · Summary** | `lid-v18-canonical-entry-summary` / `data-lid-v18-canonical-entry="field"` | **View Summary history** / `field-ready`, Summary-only, 3 events / **Back to Summary** |
| **Generated Artwork · Artwork version 2** | `lid-v18-canonical-entry-artwork` / `data-lid-v18-canonical-entry="artwork"` | **View artwork history** / `artwork-ready`, 3 events / **Back to Generated Artwork** |

The buttons use neither inherited `data-action`, runtime `data-lid-action`, nor retired `data-lid-v18-entry`. The section is a direct `<body>` child immediately after stable `#prototype-root` and immediately before stable `#modal-root`; it is outside the runtime, mutable task, inherited-record, and modal subtrees. It remains normal flow—never floating, fixed, sticky, modal, dialog, popover, or overlay—is centred at `min(1128px, calc(100% - 32px))` with positive block margins, and becomes one column at 390 and 320 px. Every button is at least 44×44 CSS pixels; the ordinary control is 52 px high and the at-most-480 px fit rule retains the 44 px floor without shrinking text. All four remain reachable in the one 568×320 page scroll. The exact active-capsule rule is `#lid-runtime-v17[data-active-feature] ~ #lid-v18-canonical-entry-panel { display: none !important; }`, so the panel and any active feature host or proof console are never exposed together.

One cached panel node is created only when absent. One observer watches only `document.body` with `{ childList: true }`; it never uses `subtree`, attributes, or character data. A queued reentrancy guard writes only to restore `prototypeRoot.nextElementSibling === panel` and `panel.nextElementSibling === modalRoot`. Correct placement causes no write; missing roots, duplicate panel IDs, or unrestorable order fail closed without creating a duplicate.

All inherited contextual controls are native and v18-ineligible. V18 does not rewrite, decorate, inject into, or intercept descendants of `#prototype-root` or `#modal-root`, and no observer reads or mutates those descendants. The exact required negative anchors are the 2 Aug **View day history**, **Before sleep — synthetic fixture** / **Revisions & provenance**, and zero v18 History controls in every Manage Reflection row. The artwork anchor is presence-based: absence before artwork generation is valid, but when represented its native text must be exactly **View versions**. The safe detached snapshot exports `artworkRepresented` and `artworkExactWhenRepresented`; the latter and aggregate `exactWhenRepresented` are true when artwork is absent and require exact **View versions** when it exists. Real native activation may toast or rerender, but leaves v18 inactive and bounded text/attribute snapshots unchanged. The detached invariant is exactly `inheritedContextPatchedCount=0`; Settings/More do not count as patched because their DOM is unchanged.

For all six origins, both Back and Escape restore the same still-connected invoking button, DOM focus, inherited view/modal state, `window.scrollY`, and its `getBoundingClientRect().top` with absolute error no greater than 1 CSS pixel. Any post-render correction is app-owned and consumed once; the evidence helper observes but never compensates. No generic fixture-return label is permitted.

Only Day and Source Item scopes show one fictional read-only **Current source context** region before the filters. The detached snapshot exports only `sourceContextVariant`, whose exact enum is `revision-2`, `correction-1`, or `none`:

- owner `upstream-revised` and any in-scope Day/Item descendant use `revision-2` and show **Fictional Revision 2 — Rain softened the path beneath the paper lanterns; the synthetic narrator counted three quiet puddles before walking home.**;
- every other Day/Item owner and its in-scope descendants use `correction-1` and show **Fictional Correction 1 — A rain-washed path curved past paper lanterns; the synthetic narrator paused beside three quiet puddles before heading home.**; and
- Global, field, artwork, hidden-day, archive, and every other non-Day/Item scope use `none` and render no source-context region.

The owner mapping survives in-scope filters, disclosure and relation focus, status unavailable, loading, failure, interruption, empty, retry, and pagination. Resetting the fixture or scope recomputes it; prose, event focus, filters, transient branches, viewport, URL, history, and storage never choose it. Neither exact prose string may occur in event/provenance text, a snapshot or summary, scenario transcript, JSON sidecar, URL/history, browser storage including OPFS, request, console/exception output, or live region. Only the visible task DOM and its PNG pixels may contain the selected prose.

The separate hidden-day view is **History for 11 August 2026**. It shows **Historical day — not shown in Calendar or Almanac**, Source `E17,E15`, and Derived `E16`; Station light note is represented at its current synthetic location of 10 Aug 2026. It is not the Deleted-upstream state and offers no restore or deletion action.

Generated-field truth is read-only: Summary version 2 is simultaneously **Current**, **Protected Field**, and **Stale**, while version 1 is **Historical**. Artwork version 2 is **Historical** and **Stale** after `E10`, not Active; both artwork versions retain **AI-generated artwork**. Event-time selection facts never become controls.

Revised, conflict, untagged, deleted, and status-unavailable presentations visibly say **Synthetic UI fixture · external evidence required**. They do not prove VoiceNotes enumeration or reconciliation. Deleted upstream leaves Monsoon walk note live on 17 Aug 2026 and **Retained locally**.

## Filters, provenance, and pagination

The exact five-filter model is **History lane**, **Record type**, **Event type**, **Attention**, and a Global-only native **Journal Date**. Filters change only after **Apply filters**; **Clear filters** restores defaults without resetting an already delivered pagination result or its generations. There is no text search, scope selector, Current/Historical selector, automatic apply, filter modal, or filter Cancel.

There is exactly one semantic filter form. It lives in one native `<details>` element at every width: at 1024 px and wider CSS hides the summary and keeps the same form visibly open in a non-scrolling 240–272 px rail; below 1024 px the native summary is visible and controls the inline disclosure. It is labelled **Filter history** or **Filter history · _n_ active**. The active-results summary and Clear remain available when the compact disclosure is closed. No duplicate wide form, modal, sheet, focus trap, filter Cancel, or filter-specific Escape behavior exists.

Each event exposes complete provenance through an inline disclosure. **Event sequence** and **Record lineage** are separate groups. Relation navigation only focuses another event or lineage fact; it does not select, activate, restore, resolve, or edit anything.

Global pagination is per lane. The partial Source page starts `E10,E14,E13,E12,E11,E05,E04` and adds `E01,E17,E15` once. The partial Derived page starts `E09,E08,E07,E06` and adds `E03,E02,E16` once. Each request carries a monotonically increasing per-lane generation; every terminal direct action or alias must supply the explicit positive exact same-lane current generation. Only the matching pending generation can settle, and only the exact next generation can represent a duplicate delivery after delivered completion. Invalid, stale, future, cross-lane, wrong-stage, and consumed terminals are strict no-ops. Pending, known failure, interruption, retry, success, duplicate delivery, and beginning-of-history states preserve the other lane, filters, disclosures, domain fingerprint, focus, and reading anchor. A successful delivery announces only **3 earlier Source events added** or **3 earlier Derived events added**. Every duplicate adds zero.

The two-lane completion summary is visible only when both unfiltered lanes added exactly three and reached their represented beginning. Any applied filter suppresses it rather than showing unfiltered totals beside filtered results. Clearing filters re-exposes the exact Source 10 / Derived 7 summary without changing delivery or generation state.

All History interactions remain read-only: `mutationIntents`, `mutationEffects`, and `providerRequests` stay zero. No control is a disguised Restore, Make current, Select, Resolve, Delete, Accept, Edit, Regenerate, or other domain mutation.

## Deterministic QA surface

The shared runtime exposes detached v18 state through:

```js
window.__LID_QA__.manifest();
window.__LID_QA__.reset();
window.__LID_QA__.setFixture("global-ready");
window.__LID_QA__.dispatch("toggle-theme");
window.__LID_QA__.snapshot();
window.__LID_QA__.runInvariants();
```

The exact top-level fixture keys, in order, are:

```text
global-ready
day-ready
item-ready
field-ready
artwork-ready
hidden-day
upstream-revised
upstream-conflict
upstream-untagged
upstream-deleted
empty
loading
failure
interrupted
```

Status unavailable, per-lane pagination, filtered empty/clear, scope-specific empties, and long-safe-metadata stress are transition branches beneath those fixtures. They are not additional manifest fixtures.

`manifest().captureScenarios` separately exposes exactly two evidence-only scenario keys, `compact-filtered-open` and `pagination-both-success`. They are disjoint from the 14-key fixture manifest and select only deterministic UI transition state for evidence capture. They add no event, domain mutation, provider request, or closable requirement.

The detached snapshot includes exact corpus and visible lane keys, filters, disclosures, relation focus, pagination state, domain facts/fingerprint, origin and Back identity, zero mutation/provider counters, and safe native-negative booleans. Under `nativeNegativeAnchors`, `artworkRepresented=false` with `artworkExactWhenRepresented=true` is the valid pre-artwork state; once the control exists, both are true only for exact native **View versions**. `runInvariants()` accepts the absent-artwork state while still checking the exact 17-event corpus and orders, 14 fixtures, scope counts, lineages, `E10` frozen-v17 truth, hidden-day separation, Source/Derived separation, read-only counters, private-prose placement, generic URL/title/history, task-before-console order, active-feature headings, and horizontal overflow.

`upstream-revised` is isolated to Source `E04,E01`, Derived none, and lineage Revision 1 → Revision 2. Revision 2 is **Displayed**, **Current upstream**, and **Revised upstream**; Revision 1 is **Historical**. Correction, Revision 3, conflict, Untagged, and Deleted facts are absent. Every fixture selection constructs fresh fixture state, resetting filters, relation focus, pagination, transient status, and all three zero counters while retaining only the selected presentation theme. Fresh `item-ready` opens exactly the `E12` provenance disclosure so frame 3 can show both lineage groups; the other 13 fixtures start with no event disclosure open. Later disclosure interaction is still in-memory and reset restores that exact fixture default.

The canonical top-level `loading` fixture is a Global initial load with `sourceContextVariant=none` and entry focus on **History & provenance**. Its only busy node is the **Source history** lane region, programmatically named by its `h2` and marked `aria-busy="true"`; inside it a static status contains `h3` **Loading history** and **Preparing separate Source and Derived event lists from synthetic browser-memory fixtures.** Derived history, events, cards, lists, final represented count, and source-context prose are absent. Page, main, filter, and scope nodes are not busy. Lane-local earlier-page loading continues to busy only the affected rendered lane. Frame 11 uses the exact 568×320 `loading`/no-scenario identity and one final passive scroll to expose that complete Source boundary within the 52–304 px safe band; it preserves the focused h1 offscreen and records unchanged DOM/CSS/ARIA, viewport, media, app-state, and domain digests.

## Evidence capture

All 16 PNG/JSON pairs use `capture-phase2-evidence-v18.mjs` against the exact query/hash-free v18 route. The frozen v17 helper remains independently hash-guarded and unchanged; the additive v18 driver is a reviewed fork/driver because the v17 executable has no import seam. `--fixture` and `--scenario` are mutually exclusive. Only frames 10 and 14 use `--scenario`; the exact allowlist is `compact-filtered-open`, then `pagination-both-success`, disjoint from `manifest().fixtures` and absent from the visible fixture console.

Start the server, then run this exact 16-frame roster from `prototypes/calendar-ui`:

```sh
V18_URL=http://127.0.0.1:4317/index-v18.html
V18_EVIDENCE_DIR=../../docs/prototypes/v18

node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-01-global-ready-wide-light.png" --meta "$V18_EVIDENCE_DIR/v18-01-global-ready-wide-light.json" --width 1440 --height 900 --view active --fixture global-ready --theme light --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-02-day-ready-wide-dark.png" --meta "$V18_EVIDENCE_DIR/v18-02-day-ready-wide-dark.json" --width 1440 --height 900 --view active --fixture day-ready --theme dark --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-03-item-conflict-medium-light.png" --meta "$V18_EVIDENCE_DIR/v18-03-item-conflict-medium-light.json" --width 960 --height 900 --view active --fixture item-ready --theme light --selector '#lid-v18-provenance-E12' --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-04-field-ready-medium-dark.png" --meta "$V18_EVIDENCE_DIR/v18-04-field-ready-medium-dark.json" --width 960 --height 900 --view active --fixture field-ready --theme dark --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-05-artwork-ready-compact-dark.png" --meta "$V18_EVIDENCE_DIR/v18-05-artwork-ready-compact-dark.json" --width 390 --height 844 --view active --fixture artwork-ready --theme dark --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-06-upstream-revised-medium-light.png" --meta "$V18_EVIDENCE_DIR/v18-06-upstream-revised-medium-light.json" --width 960 --height 900 --view active --fixture upstream-revised --theme light --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-07-upstream-untagged-compact-light.png" --meta "$V18_EVIDENCE_DIR/v18-07-upstream-untagged-compact-light.json" --width 390 --height 844 --view active --fixture upstream-untagged --theme light --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-08-upstream-deleted-compact-dark.png" --meta "$V18_EVIDENCE_DIR/v18-08-upstream-deleted-compact-dark.json" --width 390 --height 844 --view active --fixture upstream-deleted --theme dark --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-09-hidden-day-compact-light.png" --meta "$V18_EVIDENCE_DIR/v18-09-hidden-day-compact-light.json" --width 390 --height 844 --view active --fixture hidden-day --theme light --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-10-filter-open-compact-dark.png" --meta "$V18_EVIDENCE_DIR/v18-10-filter-open-compact-dark.json" --width 390 --height 844 --view active --scenario compact-filtered-open --theme dark --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-11-loading-landscape-light.png" --meta "$V18_EVIDENCE_DIR/v18-11-loading-landscape-light.json" --width 568 --height 320 --view active --fixture loading --theme light --motion reduce --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-12-interrupted-320-forced.png" --meta "$V18_EVIDENCE_DIR/v18-12-interrupted-320-forced.json" --width 320 --height 900 --view active --fixture interrupted --theme dark --motion reduce --forced-colors active
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-13-failure-medium-light.png" --meta "$V18_EVIDENCE_DIR/v18-13-failure-medium-light.json" --width 960 --height 900 --view active --fixture failure --theme light --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-14-load-earlier-wide-dark.png" --meta "$V18_EVIDENCE_DIR/v18-14-load-earlier-wide-dark.json" --width 1440 --height 900 --view active --scenario pagination-both-success --theme dark --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-15-empty-320-light.png" --meta "$V18_EVIDENCE_DIR/v18-15-empty-320-light.json" --width 320 --height 900 --view active --fixture empty --theme light --motion no-preference --forced-colors none
node capture-phase2-evidence-v18.mjs --url "$V18_URL" --out "$V18_EVIDENCE_DIR/v18-16-canonical-entry-320-forced.png" --meta "$V18_EVIDENCE_DIR/v18-16-canonical-entry-320-forced.json" --width 320 --height 900 --view archive --fixture global-ready --theme light --motion reduce --forced-colors active
```

For frame 3, `item-ready` already has only `E12` provenance open. `--selector '#lid-v18-provenance-E12'` merely scrolls that already-open disclosure into view; it does not dispatch a transition or make selector state authoritative.

The compact scenario starts from fresh complete `global-ready`, opens the actual native **Filter history** summary, changes the actual **History lane** and **Attention** controls, activates the actual **Apply filters** button, observes results focus and exactly three Source events, and reopens the actual **Filter history · 2 active** summary. Its final focus is that summary.

The pagination scenario uses QA only to seed fresh `global-ready`, enter the partial-page branch, and deliver each allowlisted synthetic success with that lane's exact current request generation. It activates the actual visible **Load earlier Source events** and **Load earlier Derived events** controls in between those QA transitions. Each delivery passively compares the logical control’s before/after `getBoundingClientRect()` top within 1 CSS pixel; the driver does not compensate the page and does not require `scrollY` equality. Focus returns to the stable Source or Derived beginning marker.

Before that unchanged five-record transcript, frame 14 runs a bounded safe-metadata-only QA2 regression and adds zero transcript records. It rejects missing/invalid/stale/future/cross-lane/consumed terminal generations as exact no-ops; accepts exact direct, alias, and settle outcomes across both lanes; proves pointer, Enter, Space, and click-only one-generation anchors plus click-only duplicate generation 2; verifies a visible bound success control; and verifies filtered Source plus Needs attention is exactly `E14,E13,E12` with the completion summary absent from both DOM and accessibility-tree inspection. Clearing restores the unfiltered 10/7 summary with an identical pagination digest. This is input-mechanics evidence only and makes no assistive-technology claim.

Frame 11 has no scenario or selector action. Its non-null `capture.framing` records the exact four loading targets, safe band, target rectangles, fixed/sticky-overlap results, sole busy Source-lane identity, exact two headings and body, `scrollX` preservation with a bounded `scrollY` change, retained offscreen h1 focus, and matching pre/post integrity digest. It neither changes layout/CSS/ARIA nor claims a visible focus indicator.

The driver requires the exact `[17,18]` runtime chain and v18 QA target. Before writing either file it validates the exact manifest order/cardinality/disjointness, real-control visibility/enabled/center hit tests, safe transcript, snapshot and invariants, unchanged domain fingerprint and zero counters, generic URL/title/null history, no horizontal overflow, localhost/data-only requests, zero local/session/IndexedDB/cache/service-worker/OPFS residue, zero console events, and zero exceptions. OPFS inspection fails closed when unavailable or inaccessible. The sidecar stores only safe `sourceContextVariant`, never either source prose.

Frame 16 uses exactly three ordered evidence-only `viewportStages`: `initial-compact` at requested and observed 320×900; `settings-wide` at requested and observed 1024×900; and `restored-compact` at requested and observed 320×900 through the final PNG. Its exact transcript is inactive compact → inactive wide → actual visible Settings **History** pointer → visible **Back to Settings** → inactive compact restored → actual visible compact More **History** pointer → Escape → canonical Day pointer/Back → Item Enter/Escape → Summary Space/Back → Artwork pointer/Escape → final PNG. Settings is measured wholly at 1024×900; More and all four canonical trips are measured wholly at 320×900.

Every resize occurs only between fully consumed trips while `activeFeature=null`, no return correction or announcement is pending, focus/scroll restoration is complete, the domain digest is unchanged, and all mutation/provider counters remain 0/0/0. Viewport never selects or changes fixture, scope, corpus, filter, disclosure, pagination, scenario, URL, history, storage, or domain state. The helper may navigate or scroll to the next real control before recording that trip's baseline; from baseline through the completed app-owned return proof it never focuses, scrolls, resizes, mutates DOM/CSS/layout, or compensates.

The bounded `archiveDiagnostics.canonicalEntry` records exact panel cardinality, body-sibling placement, copy/fact/button order, IDs, scopes, geometry, computed style, accessibility and hit tests; all six ordered round trips; `inheritedContextPatchedCount:0`; the exact two native Global-origin tokens; `otherInheritedV18OriginCount:0`; bounded 2 Aug / Before sleep native negative snapshots; hidden Settings at 320 and hidden More-launcher-family at 1024 post-V18 document-capture no-op probes; `launcherUserSurfaceAbsent:true` with visible/focusable/hit/accessibility counts all zero; and the final inactive, Artwork-focused, non-overlapping state. Its separate `originBreakpointRegression` proves the 960/961 boundary and the 1023 compact-filter coexistence on fresh pages before the unchanged frame-16 stage sequence: exact positive origin/back identity, consumed same-node return within one pixel, and opposite-origin V18 no-op with frozen passthrough unsuppressed. The origin probes require no V18 event prevention, propagation suppression, state, focus, scroll, live-region, view, modal, or toast change and separately record the unsuppressed frozen v16 passthrough. Frame 16 does not treat valid pre-artwork absence as its artwork proof: through the actual native v16 browser-memory flow it generates 2 Aug artwork when needed, reacquires the current **View versions** control, and independently requires exact text plus connected, visible, enabled, centre-hit-testable native-negative behavior before and after activation while v18 remains inactive. Every positive trip records its viewport stage, requested/observed dimensions before/active/after, visible/enabled/centre-hit/accessibility proof, before/after focus and one-pixel scroll/top measurements, digest, 0/0/0 counters, and consumed-return state. The final live-DOM checks re-prove requested and observed 320×900, desktop Settings hidden, compact More visible and reachable, exactly one canonical-panel column, no horizontal overflow, no active or pending return, visible top and bottom panel boundaries, panel height at most 744 px, fixed-surface clearance of at least 12 px, and visible focus on `#lid-v18-canonical-entry-artwork`. The helper fails closed on any mismatch.

### Required compact More keyboard regression path

At both 320×900 and 390×844, live QA must enter through the real compact More menu and its native **History** control. A helper pointer activation, direct DOM focus, QA dispatch, canonical-entry shortcut, or other synthetic substitute does not prove this path. Fresh History entry focuses `h1` **History & provenance**; native Tab then reaches the **Filter history** summary. Open that disclosure, Tab from **History lane** to **Record type**, then use Shift+Tab to traverse back from Record type to History lane and the summary. The active-v18 Tab shield stops propagation only—it never prevents the browser default or moves focus—so traversal remains native.

Exercise separate fresh runs closed through visible Back and Escape. Each must restore the same compact More **History** invoker and its focus; continuing Tab and Shift+Tab must then demonstrate the restored native More-modal focus wrap. This is a live-QA regression path only and adds no fixture, capture scenario, or evidence frame.

## Manual branch path

1. Verify the complete Global view has Source 10, Derived 7, and total 17 in the exact separate-lane order. Expand provenance and use both Event sequence and Record lineage links; confirm they move focus without changing a record state.
2. Apply every filter category, including Global Journal Date. Confirm application is explicit, compact details close after Apply, results receive focus, Clear restores defaults, and filtered empty says **No events match these filters** without changing History.
3. Open Day and Item scopes. Confirm each contains Source 8, Derived 6, total 14 and keeps Current source context readable through loading, failure, interruption, empty, filtering, and disclosure states.
4. Open Summary and Artwork. Confirm each contains only its three Derived events and the exact Current/Historical/Protected Field/Stale/AI-generated artwork state dimensions.
5. Exercise Revised, Conflict, Untagged, Deleted, and status-unavailable branches. Confirm local lineage remains visible, the external-evidence boundary is present, no event is inferred from absence, and all three counters remain zero.
6. Open 11 Aug from Global History. Confirm the exact historical-day banner, Source 2 + Derived 1, current synthetic location 10 Aug, no ordinary browse claim, and exact Back restoration.
7. Exercise both lanes independently through partial, pending, failure, retry, success, duplicate, interruption, and beginning states. Confirm exactly three additions once, no duplicates, correct announcements, stable focus/anchor, and unchanged other-lane/domain state.
8. At 960×900 prove More positive and Settings negative; at 961×900 and 1023×900 prove Settings positive and More negative while the filter presentation remains compact. Regress Settings positive / More negative at 1024×900 and More positive / Settings negative at 320×900. For each positive, confirm exact Back copy and the same connected invoker, focus, scroll, and button-top within 1 CSS pixel. For each negative, confirm the post-V18 capture boundary changes no prevention, propagation, anchor, feature, focus, scroll, state, live region, domain, view, modal, or toast, while leaving frozen behavior unsuppressed. Then enter Day, Item, Summary, and Artwork from the four canonical controls. At both 320×900 and 390×844, repeat the real keyboard-only More → History path: confirm h1 focus; Tab to Filter history; open it; Tab History lane → Record type; Shift+Tab back; and use separate Back/Escape runs to restore More History focus and native modal Tab wrap. Do not substitute helper pointer activation or direct focus. Confirm the panel is hidden while active, reappears on return, Settings is hidden and More reachable in the final compact state, and no launcher is visible, focusable, hit-testable, or accessibility-exposed.
9. Start from 2 Aug before artwork generation. In separate real-control runs, enter Global through visible Settings **History** and compact More **History**; confirm `runInvariants()` passes with exact day/source anchors, `artworkRepresented=false`, `artworkExactWhenRepresented=true`, aggregate `exactWhenRepresented=true`, no Manage Reflection History injection, and `inheritedContextPatchedCount=0`. Then generate 2 Aug artwork through the actual native v16 browser-memory flow. Confirm the post-artwork snapshot has `artworkRepresented=true` and `artworkExactWhenRepresented=true`; the current **View versions** control is connected, visible, enabled, centre-hit-testable, and exact before and after its real native activation; v18 stays inactive; and `runInvariants()` still passes. Missing artwork is a valid precondition, never a substitute for this post-artwork positive proof.
10. Re-run direct `index-v17.html` Atomic Redating success/replay paths and confirm v17 remains its own QA target while `index-v18.html` makes v18 latest.
11. Inspect 1440×900, 1024×900, 1023×900, 961×900, 960×900, 568×320, 390×844, and 320×900 in light/dark, reduced motion, and forced colours. Confirm one h1/main, logical h2/h3 order, visible focus where required, semantic lists/time/facts, minimum target sizes, no covered actions, and no horizontal page overflow. In frame 11 specifically, confirm the intentionally offscreen h1 retains focus while the complete Source loading boundary is visible.

## Deliberate limits

This static prototype does not establish durable history, backend ordering or atomicity, VoiceNotes authority, reconciliation, complete enumeration, replay safety, persistence, restart recovery, provider execution, generated-field or artwork processing, authentication, authorization, encryption, private-cache controls, export, backup/restore, deployment, operations, formal accessibility conformance, production privacy, or production readiness. It is evidence only of deterministic fictional frontend representation in one open browser page.
